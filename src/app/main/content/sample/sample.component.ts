import { Component, OnInit } from '@angular/core';

@Component({
    selector: 'sample',
    templateUrl: './sample.component.html',
    styleUrls  : ['./sample.component.scss']
})
export class SampleComponent{

    constructor(
        
        //  public postContent: PostContent,
    ) {
        // let myMuse = require('_muse');
        // postContent.render();
    }
}

